directory <- "specdata"
pollutant <- "sulfate"
id <- 1:10
# id <- c(2, 4, 8, 10, 12)
# pollutantmean(directory, pollutant, id)
# complete(directory, pollutant, id)

# threshold <- 150
# cr <- corr(directory, threshold)
# head(cr)
# summary(cr)